# the following are implicitly set based on the file name
# namespace: internal-dev
# apigee_environment: internal-dev
# apigee_organisation: nonprod
# aws_account: ptl

# for sandbox we want to use a different proxy bundle
apigee:
  proxies:
    canary-api:
      bundle: "proxies/sandbox"

aws:
  services:
    canary_api:
      environment:
        LOG_LEVEL: debug
